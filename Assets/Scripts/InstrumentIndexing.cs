using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InstrumentIndexing : MonoBehaviour
{
    public int instrumentIndexingExclusiveIndex;

    // Shakuhachi = 0
    // Euphorium = 1
    // Erhu = 2
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
